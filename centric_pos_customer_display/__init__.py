# This module is mostly assets (OWL/JS/XML/SCSS). The server-side code is a
# small public controller used by the self-service "Create account" button on the
# customer display, plus the kiosk flags on surveys.
from . import controllers
from . import models
